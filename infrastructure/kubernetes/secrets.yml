# DO NOT commit real values — populate via CI/CD or kubectl create secret
apiVersion: v1
kind: Secret
metadata:
  name: stoixima-secrets
  namespace: stoixima-prod
type: Opaque
stringData:
  TELEGRAM_API_ID: "REPLACE_ME"
  TELEGRAM_API_HASH: "REPLACE_ME"
  TELEGRAM_PHONE_NUMBER: "REPLACE_ME"
  FOOTBALL_API_KEY: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  API_KEY: "REPLACE_ME"
